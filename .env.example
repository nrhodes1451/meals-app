# Leave unset for local development: the app falls back to PGlite in .pglite/
# Set to a Neon connection string for deployed environments.
# DATABASE_URL=postgres://user:password@host/db?sslmode=require
